document.addEventListener('DOMContentLoaded', function () {
    // No se necesita manipular el DOM dinámicamente en este caso,
    // ya que el contenido está directamente en el HTML.
    // Pero puedes agregar interactividad aquí si lo deseas.
});